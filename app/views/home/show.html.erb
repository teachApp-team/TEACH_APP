<%= render 'share/header' %>
<div style="padding-top: 80px;"></div>
<div style="padding: 0 10%;">
  <h4 style="margin: 20px 0">
    勉強したい教材を選択
  </h4>
  <div class="vocabulary-text">
    <ul>
    <% Wordbook.all.each do |book| %>
      <a href="/home/level/<%= book.id %>"><li><%= book.name %></li></a>
    <% end %>
    </ul>
  </div>
</div>